{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "allow-list-classes",
    "allow-list-audio-devices",
    "allow-discover-teachers",
    "allow-capture-screen-preview"
  ]
}
