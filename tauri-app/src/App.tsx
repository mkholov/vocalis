// Deliberately empty screen — this crate is just the Tauri skeleton (React +
// TypeScript + Tailwind, bundled with Vite) for now. No commands, no state,
// no business logic wired up yet.
function App() {
  return (
    <main className="flex h-screen w-screen items-center justify-center bg-neutral-950">
      <h1 className="text-4xl font-semibold text-white">Vocalis</h1>
    </main>
  );
}

export default App;
